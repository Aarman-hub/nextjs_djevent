import React from 'react'
import Layout from '../../components/Layout'

const Detail = () => {

  const title = "Event Detail";
  return (
    <Layout title={title}>{title}</Layout>
  )
}

export default Detail